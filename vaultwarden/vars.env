DOMAIN="https://vault.mgt.zachneill.com"
SSO_ENABLED=true
SSO_AUTHORITY=https://auth.mgt.zachneill.com
SSO_TOKEN_CACHE_TTL=300
# SSO_SCOPES=profile email vaultwarden groups
SSO_SCOPES=profile email offline_access vaultwarden groups
SSO_PKCE=true
SSO_CLIENT_ID=vaultwarden
SSO_ROLES_ENABLED=true
SSO_ROLES_DEFAULT_TO_USER=true
SSO_ROLES_TOKEN_PATH=/vaultwarden_roles
# SSO_SIGNUPS_MATCH_EMAIL=true 
ROCKET_PORT=8000
SSO_ONLY=true # May need to be set to false for initial setup
SIGNUPS_ALLOWED=false
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_FROM=noreply@zachneill.com
SMTP_SECURITY=starttls
SMTP_USERNAME=noreply@zachneill.com
# Note that for this setup, Vaultwarden isn't using SSMTP's server. It's using SMTP directly

# In secrets.env:
# SSO_CLIENT_SECRET
# ADMIN_TOKEN
# SMTP_PASSWORD