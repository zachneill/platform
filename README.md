### Platform VM/platform/oupmgtplt01.mgt.zachneill.com
Platform VM on my home lab running on a laptop and two shared HDDs

Hosts monitoring stack (ALG), OIDC, Vaultwarden, and Semaphore
### Uncommitted for security: 
secrets.env 
certs/ (contains rootCA.crt, MGTWildcard.key, MGTWildcard.pem)
authelia/config/jwks.pem

Everything else is a working product